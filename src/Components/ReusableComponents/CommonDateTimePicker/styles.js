import {StyleSheet} from 'react-native';
import constants from '../../helpers/constants';

const styles = StyleSheet.create({
  cancelText: {color: '#F96B76'},
  confirmText: {color: constants.appThemeColor},
});

export default styles;
